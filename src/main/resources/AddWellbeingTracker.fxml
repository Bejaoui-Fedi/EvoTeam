<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.image.Image?>
<?import javafx.scene.effect.GaussianBlur?>
<?import javafx.scene.shape.Circle?>

<StackPane xmlns="http://javafx.com/javafx"
           xmlns:fx="http://javafx.com/fxml/1"
           fx:controller="tn.esprit.controllers.AddWellbeingTracker"
           prefWidth="600" prefHeight="750"
           stylesheets="@styles.css">

    <!-- 🌿 Background nature apaisant -->
    <ImageView fitWidth="600" fitHeight="750" preserveRatio="false" styleClass="nature-background">
        <image>
            <Image url="https://images.unsplash.com/photo-1501004318641-b39e6451bec6?w=1600&amp;q=80"
                   backgroundLoading="true"/>
        </image>
        <effect>
            <GaussianBlur radius="5"/>
        </effect>
    </ImageView>

    <!-- 🌿 Overlay -->
    <Region styleClass="overlay"/>

    <ScrollPane fitToWidth="true" style="-fx-background-color: transparent; -fx-background: transparent; -fx-padding: 20;">
        <VBox alignment="CENTER" spacing="20">

            <VBox styleClass="card" maxWidth="500" spacing="20">

                <!-- Header -->
                <VBox alignment="CENTER" spacing="5">
                    <Label text="🧘 Ajouter un suivi bien-être" styleClass="header-label"/>
                    <Label text="Enregistrez votre état du jour" styleClass="subheader-label"/>

                    <!-- Navigation buttons -->
                    <HBox spacing="10" alignment="CENTER" style="-fx-padding: 10 0 0 0;">
                        <Button text="📋 Liste des suivis"
                                onAction="#goToDisplay"
                                styleClass="button-secondary"/>
                        <Button text="🏠 Dashboard"
                                onAction="#goToDashboard"
                                styleClass="button-secondary"/>
                    </HBox>
                </VBox>

                <Separator/>

                <!-- 🔍 RECHERCHE UTILISATEUR AVANCÉE -->
                <VBox spacing="5" style="-fx-padding: 0 0 5 0;">
                    <Label text="👤 Rechercher un utilisateur *" style="-fx-font-weight: bold; -fx-text-fill: #2C3E50;"/>
                    <HBox spacing="5" alignment="CENTER_LEFT">
                        <TextField fx:id="searchUserField"
                                   promptText="Tapez le nom ou l'email..."
                                   HBox.hgrow="ALWAYS"
                                   prefHeight="40"
                                   styleClass="search-field"/>
                        <Button fx:id="clearUserBtn"
                                text="✖"
                                styleClass="button-secondary"
                                visible="false" managed="false"/>
                    </HBox>
                    <ListView fx:id="userSuggestionsList"
                              prefHeight="120"
                              visible="false" managed="false"
                              style="-fx-background-color: white; -fx-border-color: #3A7D6B; -fx-border-radius: 5;"/>
                    <Label fx:id="selectedUserLabel"
                           text="Aucun utilisateur sélectionné"
                           style="-fx-text-fill: #7f8c8d; -fx-font-style: italic; -fx-padding: 5 0 0 0;"/>
                </VBox>

                <!-- 🔍 RECHERCHE TÂCHE AVANCÉE -->
                <VBox spacing="5" style="-fx-padding: 0 0 5 0;">
                    <Label text="✅ Rechercher une tâche *" style="-fx-font-weight: bold; -fx-text-fill: #2C3E50;"/>
                    <HBox spacing="5" alignment="CENTER_LEFT">
                        <TextField fx:id="searchTaskField"
                                   promptText="Tapez le titre de la tâche..."
                                   HBox.hgrow="ALWAYS"
                                   prefHeight="40"
                                   styleClass="search-field"/>
                        <Button fx:id="clearTaskBtn"
                                text="✖"
                                styleClass="button-secondary"
                                visible="false" managed="false"/>
                    </HBox>
                    <ListView fx:id="taskSuggestionsList"
                              prefHeight="120"
                              visible="false" managed="false"
                              style="-fx-background-color: white; -fx-border-color: #3A7D6B; -fx-border-radius: 5;"/>
                    <Label fx:id="selectedTaskLabel"
                           text="Aucune tâche sélectionnée"
                           style="-fx-text-fill: #7f8c8d; -fx-font-style: italic; -fx-padding: 5 0 0 0;"/>
                </VBox>

                <!-- Original ComboBoxes (cachés mais conservés pour la compatibilité) -->
                <ComboBox fx:id="cbUserId" visible="false" managed="false"/>
                <ComboBox fx:id="cbRoutineTaskId" visible="false" managed="false"/>

                <!-- Date -->
                <VBox spacing="5">
                    <Label text="📅 Date du suivi *" styleClass="normal-label" style="-fx-font-weight: bold;"/>
                    <DatePicker fx:id="datePicker" styleClass="text-field" prefHeight="35"/>
                </VBox>

                <!-- Indicateurs (Humeur, Stress, Énergie) -->
                <HBox spacing="10" alignment="CENTER_LEFT">
                    <VBox spacing="5" HBox.hgrow="ALWAYS">
                        <Label text="😊 Humeur *" styleClass="normal-label" style="-fx-font-weight: bold;"/>
                        <ComboBox fx:id="cbMood" promptText="1-5" styleClass="text-field" prefWidth="140"/>
                    </VBox>
                    <VBox spacing="5" HBox.hgrow="ALWAYS">
                        <Label text="😰 Stress *" styleClass="normal-label" style="-fx-font-weight: bold;"/>
                        <ComboBox fx:id="cbStress" promptText="1-5" styleClass="text-field" prefWidth="140"/>
                    </VBox>
                    <VBox spacing="5" HBox.hgrow="ALWAYS">
                        <Label text="⚡ Énergie *" styleClass="normal-label" style="-fx-font-weight: bold;"/>
                        <ComboBox fx:id="cbEnergy" promptText="1-5" styleClass="text-field" prefWidth="140"/>
                    </VBox>
                </HBox>

                <!-- Heures de sommeil -->
                <VBox spacing="5">
                    <Label text="😴 Heures de sommeil *" styleClass="normal-label" style="-fx-font-weight: bold;"/>
                    <TextField fx:id="tfSleepHours" promptText="Ex: 7.5" styleClass="text-field"/>
                </VBox>

                <!-- Note optionnelle -->
                <VBox spacing="5">
                    <Label text="📝 Note (optionnelle)" styleClass="normal-label" style="-fx-font-weight: bold;"/>
                    <TextArea fx:id="taNote" promptText="Comment vous sentez-vous aujourd'hui ?"
                              prefRowCount="3" wrapText="true" styleClass="text-field"/>
                </VBox>

                <!-- Boutons d'action -->
                <VBox spacing="10" alignment="CENTER" style="-fx-padding: 10 0 0 0;">
                    <Button text="✅ Enregistrer"
                            onAction="#handleAddTracker"
                            prefWidth="300" prefHeight="40"
                            styleClass="button-primary"/>

                    <Button text="🔄 Réinitialiser"
                            onAction="#resetForm"
                            prefWidth="300" prefHeight="40"
                            styleClass="button-secondary"/>
                </VBox>

                <!-- Champs obligatoires -->
                <HBox alignment="CENTER">
                    <Label text="* Champs obligatoires"
                           style="-fx-text-fill: #E74C3C; -fx-font-size: 11; -fx-background-color: rgba(255,255,255,0.5); -fx-padding: 3 10; -fx-background-radius: 20;"/>
                </HBox>

            </VBox>
        </VBox>
    </ScrollPane>

</StackPane>