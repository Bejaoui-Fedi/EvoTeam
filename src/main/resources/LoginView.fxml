<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.image.Image?>
<?import javafx.scene.effect.GaussianBlur?>

<StackPane xmlns="http://javafx.com/javafx"
           xmlns:fx="http://javafx.com/fxml/1"
           fx:controller="tn.esprit.controllers.LoginController"
           prefWidth="900" prefHeight="600"
           stylesheets="@styles.css">

    <!-- 🌿 Background nature apaisant -->
    <ImageView fitWidth="900" fitHeight="600" preserveRatio="false" styleClass="nature-background">
        <image>
            <Image url="https://images.unsplash.com/photo-1501004318641-b39e6451bec6?w=1600&amp;q=80"
                   backgroundLoading="true"/>
        </image>
        <effect>
            <GaussianBlur radius="5"/>
        </effect>
    </ImageView>

    <!-- 🌿 Overlay vert pistache très doux -->
    <Region style="-fx-background-color: rgba(225, 240, 218, 0.85);"/>

    <!-- Login Card -->
    <VBox alignment="CENTER" maxWidth="450" maxHeight="550" spacing="20" styleClass="card">
        
        <Label text="🌿" style="-fx-font-size: 48; -fx-text-fill: #3A7D6B;"/>
        
        <Label fx:id="titleLabel" text="Connexion" styleClass="header-label"/>
        <Label fx:id="subtitleLabel" text="Accédez à votre espace de sérénité" styleClass="subheader-label"/>

        <Label fx:id="messageLabel" wrapText="true" visible="false" managed="false"
               style="-fx-text-fill: #e74c3c; -fx-background-color: #fadbd8; -fx-padding: 10; -fx-background-radius: 5;"/>

        <VBox spacing="15" alignment="CENTER_LEFT" style="-fx-padding: 0 20 0 20;">
            <Label text="Email" styleClass="normal-label"/>
            <TextField fx:id="emailField" promptText="exemple@email.com" styleClass="text-field"/>

            <Label text="Mot de passe" styleClass="normal-label"/>
            <PasswordField fx:id="passwordField" promptText="Votre mot de passe" styleClass="text-field"/>
        </VBox>

        <HBox alignment="CENTER_RIGHT" spacing="10" style="-fx-padding: 0 20 0 20;">
            <CheckBox fx:id="rememberCheckBox" text="Se souvenir de moi" styleClass="normal-label"/>
            <Region HBox.hgrow="ALWAYS"/>
            <Hyperlink text="Mot de passe oublié ?" onAction="#handleForgotPassword" styleClass="button-link"/>
        </HBox>

        <Button text="🍃 Se connecter" onAction="#handleLogin"
                prefWidth="300" prefHeight="45" styleClass="button-primary"/>

        <HBox alignment="CENTER" spacing="5">
            <Label text="Pas encore de compte ?" styleClass="normal-label"/>
            <Hyperlink text="Inscrivez-vous ici" onAction="#handleRegisterLink" styleClass="button-link"/>
        </HBox>
        
        <Separator style="-fx-padding: 10 0 10 0;"/>
        
        <Button text="🏠 Retour à l'accueil" onAction="#handleBackToHome"
                prefWidth="200" styleClass="button-secondary"/>

    </VBox>

</StackPane>