<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="tn.esprit.controllers.LoginController"
            prefWidth="450" prefHeight="550">

    <top>
        <VBox fx:id="headerContainer" style="-fx-background-color: linear-gradient(to right, #2C3E50, #3498DB); -fx-padding: 30;">
            <Label fx:id="titleLabel" text="Connexion" textFill="WHITE" alignment="CENTER">
                <font>
                    <Font name="System Bold" size="28.0"/>
                </font>
            </Label>
            <Label fx:id="subtitleLabel" text="Accédez à votre espace personnel" textFill="#ECF0F1" alignment="CENTER"
                   style="-fx-font-size: 14;"/>
        </VBox>
    </top>

    <center>
        <VBox spacing="20" style="-fx-padding: 40;" alignment="TOP_CENTER">
            <Label fx:id="messageLabel" wrapText="true" visible="false" managed="false"
                   style="-fx-padding: 15; -fx-background-radius: 5; -fx-font-size: 13;"/>

            <GridPane hgap="15" vgap="15" alignment="CENTER">
                <columnConstraints>
                    <ColumnConstraints percentWidth="30"/>
                    <ColumnConstraints percentWidth="70"/>
                </columnConstraints>
                <Label text="Email :" GridPane.rowIndex="0" GridPane.columnIndex="0"
                       style="-fx-font-weight: bold; -fx-font-size: 14;"/>
                <TextField fx:id="emailField" promptText="exemple@email.com"
                           GridPane.rowIndex="0" GridPane.columnIndex="1"
                           style="-fx-font-size: 14; -fx-pref-height: 40; -fx-background-radius: 5;"/>
                <Label text="Mot de passe :" GridPane.rowIndex="1" GridPane.columnIndex="0"
                       style="-fx-font-weight: bold; -fx-font-size: 14;"/>
                <PasswordField fx:id="passwordField" promptText="Votre mot de passe"
                               GridPane.rowIndex="1" GridPane.columnIndex="1"
                               style="-fx-font-size: 14; -fx-pref-height: 40; -fx-background-radius: 5;"/>
            </GridPane>

            <HBox alignment="CENTER_RIGHT" spacing="10">
                <CheckBox fx:id="rememberCheckBox" text="Se souvenir de moi"/>
                <Hyperlink text="Mot de passe oublié ?" onAction="#handleForgotPassword"
                           style="-fx-font-size: 12;"/>
            </HBox>
        </VBox>
    </center>

    <bottom>
        <VBox spacing="15" style="-fx-padding: 30; -fx-background-color: #F8F9FA;">
            <!-- Bouton Se connecter -->
            <Button text="🔐 Se connecter" onAction="#handleLogin"
                    prefWidth="Infinity" prefHeight="45"
                    style="-fx-background-color: #3498DB; -fx-text-fill: white; -fx-font-size: 15; -fx-font-weight: bold; -fx-background-radius: 5; -fx-cursor: hand;"/>

            <!-- Lien Inscription -->
            <HBox alignment="CENTER" spacing="5">
                <Label text="Pas encore de compte ?" style="-fx-font-size: 13;"/>
                <Hyperlink text="Inscrivez-vous ici" onAction="#handleRegisterLink"
                           style="-fx-font-size: 13; -fx-font-weight: bold; -fx-text-fill: #27AE60;"/>
            </HBox>

            <!-- Séparateur -->
            <Separator/>

            <!-- Bouton Retour à l'accueil -->
            <Button text="🏠 Retour à l'accueil" onAction="#handleBackToHome"
                    prefWidth="Infinity" prefHeight="40"
                    style="-fx-background-color: #95A5A6; -fx-text-fill: white; -fx-font-size: 14; -fx-font-weight: bold; -fx-background-radius: 5; -fx-cursor: hand;"/>
        </VBox>
    </bottom>

</BorderPane>