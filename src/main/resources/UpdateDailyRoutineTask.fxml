<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.image.Image?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>
<?import javafx.scene.effect.GaussianBlur?>

<StackPane xmlns="http://javafx.com/javafx"
           xmlns:fx="http://javafx.com/fxml/1"
           fx:controller="tn.esprit.controllers.UpdateDailyRoutineTask"
           prefWidth="600" prefHeight="850"
           stylesheets="@styles.css">

    <!-- 🌿 Background nature apaisant -->
    <ImageView fitWidth="600" fitHeight="850" preserveRatio="false" styleClass="nature-background">
        <image>
            <Image url="https://images.unsplash.com/photo-1501004318641-b39e6451bec6?w=1600&amp;q=80"
                   backgroundLoading="true"/>
        </image>
        <effect>
            <GaussianBlur radius="5"/>
        </effect>
    </ImageView>

    <!-- 🌿 Overlay -->
    <Region styleClass="overlay"/>

    <ScrollPane fitToWidth="true" style="-fx-background-color: transparent; -fx-background: transparent; -fx-padding: 20;">
        <VBox alignment="CENTER" spacing="20">

            <VBox styleClass="card" maxWidth="500" spacing="20">

                <!-- Header -->
                <VBox alignment="CENTER" spacing="5">
                    <HBox alignment="CENTER" spacing="10">
                        <Label text="✏️" style="-fx-font-size: 32; -fx-text-fill: #3A7D6B;"/>
                        <Label text="Modifier la tâche" styleClass="header-label" style="-fx-font-size: 24;"/>
                    </HBox>
                    <Label text="Modifiez votre tâche quotidienne" styleClass="subheader-label"/>

                    <!-- ID Badge -->
                    <Label fx:id="lblId" text="ID: -"
                           style="-fx-text-fill: #7F8C8D; -fx-font-size: 12; -fx-font-weight: bold;
                                  -fx-background-color: rgba(255,255,255,0.5); -fx-padding: 3 10;
                                  -fx-background-radius: 20; -fx-margin: 5 0 0 0;"/>
                </VBox>

                <Separator/>

                <!-- 💡 QUOTE BUTTON - Like in AddDailyRoutineTask -->
                <VBox spacing="5" style="-fx-padding: 0 0 5 0;">
                    <HBox alignment="CENTER" spacing="10">
                        <Button fx:id="btnSuggestQuote"
                                text="💡 Suggérer une citation inspirante"
                                onAction="#suggestQuoteTask"
                                prefWidth="380" prefHeight="45"
                                styleClass="button-primary"
                                style="-fx-background-color: #9B59B6; -fx-font-size: 14; -fx-font-weight: bold;"/>
                    </HBox>
                    <HBox alignment="CENTER">
                        <Label text="Cliquez pour générer une citation inspirante pour votre tâche"
                               style="-fx-text-fill: #6F9A8D; -fx-font-size: 11; -fx-font-style: italic;"/>
                    </HBox>
                </VBox>

                <Separator/>

                <!-- 🔍 RECHERCHE UTILISATEUR AVANCÉE -->
                <VBox spacing="5" style="-fx-padding: 0 0 5 0;">
                    <Label text="👤 Rechercher un utilisateur *" style="-fx-font-weight: bold; -fx-text-fill: #2C3E50;"/>
                    <HBox spacing="5" alignment="CENTER_LEFT">
                        <TextField fx:id="searchUserField"
                                   promptText="Tapez le nom ou l'email..."
                                   HBox.hgrow="ALWAYS"
                                   prefHeight="40"
                                   styleClass="search-field"/>
                        <Button fx:id="clearUserBtn"
                                text="✖"
                                styleClass="button-secondary"
                                visible="false" managed="false"/>
                    </HBox>
                    <ListView fx:id="userSuggestionsList"
                              prefHeight="100"
                              visible="false" managed="false"
                              style="-fx-background-color: white; -fx-border-color: #3A7D6B; -fx-border-radius: 5; -fx-background-radius: 5;"/>
                    <Label fx:id="selectedUserLabel"
                           text="Aucun utilisateur sélectionné"
                           style="-fx-text-fill: #7f8c8d; -fx-font-style: italic; -fx-padding: 5 0 0 0;"/>
                </VBox>

                <!-- Original ComboBox (caché mais conservé pour la compatibilité) -->
                <ComboBox fx:id="cbUserId" visible="false" managed="false"/>

                <!-- Titre - TEXTAREA for multi-line (like in AddDailyRoutineTask) -->
                <VBox spacing="5">
                    <Label text="📌 Titre de la tâche *" styleClass="normal-label" style="-fx-font-weight: bold;"/>
                    <TextArea fx:id="tfTitle"
                              prefHeight="120"
                              wrapText="true"
                              promptText="Entrez le titre de la tâche..."
                              styleClass="text-field"
                              style="-fx-background-radius: 10; -fx-padding: 10;"/>
                    <Label text="Les citations apparaîtront sur plusieurs lignes (quote, author, category)"
                           style="-fx-text-fill: #6F9A8D; -fx-font-size: 11; -fx-font-style: italic;"/>
                </VBox>

                <!-- Statut -->
                <VBox spacing="5">
                    <Label text="✅ Statut" styleClass="normal-label" style="-fx-font-weight: bold;"/>
                    <HBox alignment="CENTER_LEFT" spacing="20" style="-fx-padding: 5 0;">
                        <CheckBox fx:id="chkCompleted" text="Tâche complétée"
                                  style="-fx-font-size: 14; -fx-text-fill: #2C3E50;"/>
                    </HBox>
                </VBox>

                <!-- Dates -->
                <VBox spacing="10" style="-fx-background-color: #F8F9FA; -fx-padding: 15; -fx-background-radius: 10;">
                    <HBox spacing="10" alignment="CENTER_LEFT">
                        <Label text="📅 Complétée le:" style="-fx-font-size: 13; -fx-font-weight: bold; -fx-text-fill: #555;"/>
                        <Label fx:id="lblCompletedAt" text="-"
                               style="-fx-font-size: 13; -fx-font-style: italic; -fx-text-fill: #7F8C8D;"/>
                    </HBox>

                    <HBox spacing="10" alignment="CENTER_LEFT">
                        <Label text="📅 Créée le:" style="-fx-font-size: 13; -fx-font-weight: bold; -fx-text-fill: #555;"/>
                        <Label fx:id="lblCreatedAt" text="-"
                               style="-fx-font-size: 13; -fx-font-style: italic; -fx-text-fill: #7F8C8D;"/>
                    </HBox>
                </VBox>

                <!-- Boutons d'action -->
                <VBox spacing="10" alignment="CENTER" style="-fx-padding: 10 0 0 0;">
                    <Button text="✅ Enregistrer les modifications"
                            onAction="#handleUpdate"
                            prefWidth="300" prefHeight="45"
                            styleClass="button-primary"
                            style="-fx-background-color: #F39C12;"/>

                    <HBox spacing="10" alignment="CENTER">
                        <Button text="❌ Annuler"
                                onAction="#closeWindow"
                                prefWidth="145" prefHeight="40"
                                styleClass="button-secondary"/>

                        <Button text="📋 Liste des tâches"
                                onAction="#goToDisplay"
                                prefWidth="145" prefHeight="40"
                                styleClass="button-primary"
                                style="-fx-background-color: #3498DB;"/>
                    </HBox>
                </VBox>

                <!-- Champs obligatoires -->
                <HBox alignment="CENTER">
                    <Label text="* Champs obligatoires"
                           style="-fx-text-fill: #E74C3C; -fx-font-size: 11; -fx-background-color: rgba(255,255,255,0.5); -fx-padding: 3 10; -fx-background-radius: 20;"/>
                </HBox>

                <!-- Info footer -->
                <HBox alignment="CENTER" spacing="5" style="-fx-padding: 5 0 0 0;">
                    <Label text="ℹ️" style="-fx-font-size: 14; -fx-text-fill: #3A7D6B;"/>
                    <Label text="Toutes les tâches sont horodatées automatiquement"
                           style="-fx-font-size: 11; -fx-text-fill: #6F9A8D; -fx-font-style: italic;"/>
                </HBox>

            </VBox>
        </VBox>
    </ScrollPane>

</StackPane>