<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.image.Image?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>
<?import javafx.scene.effect.GaussianBlur?>

<StackPane xmlns="http://javafx.com/javafx"
           xmlns:fx="http://javafx.com/fxml/1"
           fx:controller="tn.esprit.controllers.UpdateWellbeingTracker"
           prefWidth="600" prefHeight="750"
           stylesheets="@styles.css">

    <!-- 🌿 Background nature apaisant -->
    <ImageView fitWidth="600" fitHeight="750" preserveRatio="false" styleClass="nature-background">
        <image>
            <Image url="https://images.unsplash.com/photo-1501004318641-b39e6451bec6?w=1600&amp;q=80"
                   backgroundLoading="true"/>
        </image>
        <effect>
            <GaussianBlur radius="5"/>
        </effect>
    </ImageView>

    <!-- 🌿 Overlay -->
    <Region styleClass="overlay"/>

    <ScrollPane fitToWidth="true" style="-fx-background-color: transparent; -fx-background: transparent; -fx-padding: 20;">
        <VBox alignment="CENTER" spacing="20">

            <VBox styleClass="card" maxWidth="500" spacing="20">

                <!-- Header -->
                <VBox alignment="CENTER" spacing="5">
                    <HBox alignment="CENTER" spacing="10">
                        <Label text="✏️" style="-fx-font-size: 32; -fx-text-fill: #3A7D6B;"/>
                        <Label text="Modifier le suivi bien-être" styleClass="header-label" style="-fx-font-size: 24;"/>
                    </HBox>
                    <Label text="Modifiez votre état du jour" styleClass="subheader-label"/>

                    <!-- ID Badge -->
                    <Label fx:id="lblId" text="ID: -"
                           style="-fx-text-fill: #7F8C8D; -fx-font-size: 12; -fx-font-weight: bold;
                                  -fx-background-color: rgba(255,255,255,0.5); -fx-padding: 3 10;
                                  -fx-background-radius: 20; -fx-margin: 5 0 0 0;"/>
                </VBox>

                <Separator/>

                <!-- Utilisateur -->
                <VBox spacing="5">
                    <Label text="👤 Utilisateur *" styleClass="normal-label" style="-fx-font-weight: bold;"/>
                    <ComboBox fx:id="cbUserId" prefHeight="40" promptText="Sélectionner un utilisateur" styleClass="text-field"/>
                </VBox>

                <!-- Tâche de routine -->
                <VBox spacing="5">
                    <Label text="✅ Tâche de routine *" styleClass="normal-label" style="-fx-font-weight: bold;"/>
                    <ComboBox fx:id="cbRoutineTaskId" prefHeight="40" promptText="Sélectionner une tâche" styleClass="text-field"/>
                </VBox>

                <!-- Date -->
                <VBox spacing="5">
                    <Label text="📅 Date du suivi *" styleClass="normal-label" style="-fx-font-weight: bold;"/>
                    <DatePicker fx:id="datePicker" prefHeight="40" styleClass="text-field"/>
                </VBox>

                <!-- Indicateurs (Humeur, Stress, Énergie) -->
                <HBox spacing="10" alignment="CENTER_LEFT">
                    <VBox spacing="5" HBox.hgrow="ALWAYS">
                        <Label text="😊 Humeur *" styleClass="normal-label" style="-fx-font-weight: bold; -fx-font-size: 13;"/>
                        <ComboBox fx:id="cbMood" promptText="1-5" styleClass="text-field" prefWidth="140" prefHeight="40"/>
                    </VBox>
                    <VBox spacing="5" HBox.hgrow="ALWAYS">
                        <Label text="😰 Stress *" styleClass="normal-label" style="-fx-font-weight: bold; -fx-font-size: 13;"/>
                        <ComboBox fx:id="cbStress" promptText="1-5" styleClass="text-field" prefWidth="140" prefHeight="40"/>
                    </VBox>
                    <VBox spacing="5" HBox.hgrow="ALWAYS">
                        <Label text="⚡ Énergie *" styleClass="normal-label" style="-fx-font-weight: bold; -fx-font-size: 13;"/>
                        <ComboBox fx:id="cbEnergy" promptText="1-5" styleClass="text-field" prefWidth="140" prefHeight="40"/>
                    </VBox>
                </HBox>

                <!-- Heures de sommeil -->
                <VBox spacing="5">
                    <Label text="😴 Heures de sommeil *" styleClass="normal-label" style="-fx-font-weight: bold;"/>
                    <TextField fx:id="tfSleepHours" promptText="Ex: 7.5" styleClass="text-field" prefHeight="40"/>
                </VBox>

                <!-- Note optionnelle -->
                <VBox spacing="5">
                    <Label text="📝 Note (optionnelle)" styleClass="normal-label" style="-fx-font-weight: bold;"/>
                    <TextArea fx:id="taNote" promptText="Comment vous sentez-vous aujourd'hui ?"
                              prefRowCount="3" wrapText="true" styleClass="text-field"/>
                </VBox>

                <!-- Métadonnées (création) -->
                <HBox spacing="5" alignment="CENTER_LEFT" style="-fx-padding: 5 0 0 0;">
                    <Label text="📅 Créé le:" style="-fx-font-size: 11; -fx-text-fill: #7F8C8D;"/>
                    <Label fx:id="lblCreatedAt" text="-"
                           style="-fx-font-size: 11; -fx-font-style: italic; -fx-text-fill: #7F8C8D;"/>
                </HBox>

                <!-- Boutons d'action -->
                <VBox spacing="10" alignment="CENTER" style="-fx-padding: 10 0 0 0;">
                    <Button text="✅ Enregistrer les modifications"
                            onAction="#handleUpdate"
                            prefWidth="300" prefHeight="45"
                            styleClass="button-primary"/>

                    <HBox spacing="10" alignment="CENTER">
                        <Button text="❌ Annuler"
                                onAction="#closeWindow"
                                prefWidth="145" prefHeight="40"
                                styleClass="button-secondary"/>
                    </HBox>
                </VBox>

                <!-- Champs obligatoires -->
                <HBox alignment="CENTER">
                    <Label text="* Champs obligatoires"
                           style="-fx-text-fill: #E74C3C; -fx-font-size: 11; -fx-background-color: rgba(255,255,255,0.5); -fx-padding: 3 10; -fx-background-radius: 20;"/>
                </HBox>

            </VBox>
        </VBox>
    </ScrollPane>

</StackPane>